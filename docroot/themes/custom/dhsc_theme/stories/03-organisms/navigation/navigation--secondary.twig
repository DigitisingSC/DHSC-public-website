{% set classes = [
  'o-navigation-region',
  'o-navigation-region--secondary',
  'bg-white text-black'
] | sort | join(' ') | trim %}

{% set attributes = attributes
  .addClass(classes)
%}

<section{{ attributes }}>
  <div class="container mx-auto w-full py-4">
    {{ content }}
  </div>
</section>
