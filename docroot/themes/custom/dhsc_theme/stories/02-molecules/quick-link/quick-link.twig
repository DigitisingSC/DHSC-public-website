{#
  Template
#}

{# Classes #}
{% set classes = [
  'm-quick-link',
  'm-quick-link--' ~ quickLinkType|default('bg-green-20'),
  'relative rounded-md'
] | join(' ') | trim %}

{% set attributes = attributes
  .addClass(classes)
%}

<div {{ attributes }}>
  {% if image %}
    <div class="m-quick-link__image">
      {{ image }}
    </div>
  {% endif %}
  <div class="content-wrapper flex flex-col justify-between h-full p-4 tablet:p-6 ">
    {% if heading %}
      <h3>{{ heading }}</h3>
    {% endif %}
    {% if description %}
      <div class="grow">{{ description }}</div>
    {% endif %}
    <p class="flex justify-end pt-4">
      <svg width="20" height="20" viewBox="0 0 20 20" fill="none" xmlns="http://www.w3.org/2000/svg"><path d="M10 20C15.5228 20 20 15.5228 20 10C20 4.47715 15.5228 0 10 0C4.47715 0 0 4.47715 0 10C0 15.5228 4.47715 20 10 20Z" fill="#00A189"/><path d="M9 7L12 10L9 13" stroke="#E5EEED" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/></svg>
    </p>
    <a href="{{ link }}" class="absolute top-0 bottom-0 left-0 right-0"></a>
  </div>
</div>
