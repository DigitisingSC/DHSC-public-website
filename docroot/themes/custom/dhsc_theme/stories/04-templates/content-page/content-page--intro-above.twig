{#
  Template
#}

{% set classes = [
  't-content-page',
	't-content-page--intro-above'
] %}

<article{{attributes.addClass(classes)}}>
  {% if intro %}
    <div class="t-content-page__intro">
      {{ intro }}
    </div>
  {% endif %}
  {% if title %}
    <div class="t-content-page__title">
      <h1 class="mb-4">{{ title }}</h1>
    </div>
  {% endif %}
  {% if content %}
    <div class="t-content-page__content">
      {{ content }}
    </div>
  {% endif %}
</article>
